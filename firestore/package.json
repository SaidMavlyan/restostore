{
  "name": "firestore",
  "private": true,
  "scripts": {
    "deploy": "firebase deploy --only firestore",
    "emulator": "firebase emulators:start --only firestore",
    "watch": "mocha-typescript-watch ./tests",
    "pretest": "tsc",
    "test": "mocha ./tests --timeout=10000"
  },
  "devDependencies": {
    "@firebase/testing": "^0.15.0",
    "@types/mocha": "5.2.5",
    "mocha": "5.2.0",
    "mocha-typescript": "1.1.17",
    "source-map-support": "0.5.9",
    "ts-node": "7.0.1",
    "typescript": "3.1.3"
  }
}
